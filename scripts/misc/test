#! /usr/bin/env python3

import uweclang
from pprint import pprint

text = 'This is b)))()()()()()((((()(())efore, (in the middle, (deep()er)), and after '
tree = uweclang.extract_parentheticals(text)

pprint(tree)
