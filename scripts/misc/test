#! /usr/bin/env python3

import uweclang
from pprint import pprint

text = 'ab)c((d)ef)g()(hi'
tree = uweclang.parse_parentheticals(text)
print(text)
pprint(tree)



print(uweclang.recombine_parentheticals(tree))
